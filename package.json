{
  "name": "heroku-json-server",
  "version": "1.0.0",
  "description": "->npm install -g heroku\r ->heroku --version\r ->npm init -> start file(main) -> server.js\r ->npm i json-server\r ->manually create .gitignore,server.js,README.md and paste the codes already which is there in the file\r ->create db.json\r ->Initialize GIT\r ->Create a heroku login keep signed in\r ->heroku login in Terminal\r ->heroku create heroku-json-server-fakedb\r ->Have everything up-to-date in your application - maintain GIT\r ->git push heroku master\r ->heroku open\r ------------------------------------------------\r npm install firebase\r npm audit fix --force\r npm install -g firebase-tools",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
